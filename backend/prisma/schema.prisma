generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Placeholder model - required by Prisma to generate client. Remove when adding real models.
model SchemaPlaceholder {
  id Int @id @default(autoincrement())
}
